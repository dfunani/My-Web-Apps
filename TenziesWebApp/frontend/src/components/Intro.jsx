import { Button, Row, Container, Col } from "react-bootstrap"

export default function Intro(props)
{
	return (
	<div className="App-Intro">
        <div className='App-Intro row w-50'>
          <p className='col w-50 text-center'>Roll until all dice are the same. Click each die to freeze it at its current value between rolls.</p>
        </div>
		</div>
	)
}